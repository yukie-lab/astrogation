# Project ASTROGATION — 憲法

> このファイルはセッションを跨いで不変。変更権限は人間のみ。
> フェーズごとの作業は `docs/phases/PHASE_*.md` の指示書に従う。
> 現在の許可フェーズ: **Phase L**(人間がゲート承認するまで次へ進むな)

---

## 1. MISSION

ワープドライブを作るのでも、定理を証明するのでもない。

**Le の正エネルギー・ワープ殻理論(arXiv:2606.22531)の許容性制約を
すべて課した恒星間機動の全域カタログと、その観測署名(光度曲線・
角度パターン)を、世界で初めて計算する。**

航路表と観測署名は同じ計算の表裏である:ミッションプロファイル
(m(u), v(u), x(u)) を積分すれば、光度曲線 L(u) = −ṁ は副産物として出る。

## 2. ORACLES — 真実の源(この優先順)

1. **Le, arXiv:2606.22531 v3**(2026-07-15)`papers/le_steering_v3.pdf`
   本文・付録A〜Kが第一のオラクル。式番号は必ず v3 基準で引く
   (v2→v3 で式番号がずれている。例: Tsiolkovsky は v2:(14) → v3:(15)。
   Phase L で全対応表を作るまで、式番号の暗記に依存するな)
2. **Le, arXiv:2605.25417**(境界コスト論文)— 背景と対比に使う
3. **Füzfa 2019, PRD 99, 104081**(arXiv:1902.03869)— 点ロケット
   運動学の先行研究。**差分の相手**であり、再発明の対象ではない
4. **warpax v1.3.0** `~/research/warpax`(導入済み)— `warpax.bondi`
   を独立クロスチェック経路として使用可
5. **github.com/anindex/world_tube** — 二次オラクル(著者コード。
   論文と同格ではない)

論文にない主張・補間・近似をコードに入れる場合は、必ず
`ASSUMPTIONS.md` に番号付きで記録し、根拠と影響範囲を書く。

## 3. CONVENTION LEDGER — 規約台帳

すべての規約は `conventions.md` に一元管理する(Phase 0 で作成)。
台帳の変更は人間の承認必須。最低限、以下を固定する:

- G = c = 1 / 計量符号 (−,+,+,+) / κ = 8π(Le・warpax と同一)
- θ は**瞬間静止系の加速軸から**測る(前方極 θ=0 で n² 最小、
  後方極 θ=π が排気)
- u は遅延時間 / x = 2m/R / λ = aR / x_eff = x + 2λ
- 実装する式には**論文の式番号(v3)をコード内コメントで併記**
- 単位換算(幾何単位 ↔ SI ↔ 天文単位)は換算モジュール一箇所に集約

符号・因子・角度規約の事故は今回の最大リスクである。
規約に触れる判断を台帳の外で行ってはならない。

## 4. CERTIFICATION — G1: 認証なしの式は存在しないのと同じ

式を一つ実装するたびに、**論文記載の数値を再現する pytest テスト**を
書く。認証に合格するまで、その式を下流の計算に使ってはならない。

必須認証セット(式番号は v2 基準。Phase L で v3 に再マップせよ):

| ID | 内容 | 合格基準 |
|---|---|---|
| C1 | Tsiolkovsky: m_f/m₀ = e^(−3Δη) | 相対誤差 < 1e-12 |
| C2 | 表面DEC窓: σ₀−p₀ の符号が x=24/25 で反転、窓内で正 (v2: Eq.17-18) | 窓境界で厳密ゼロ交差 |
| C3 | ℓ≤1/ℓ=2 閉包の非退化(C3a–C3d: v3 (77),(79),(80),(81),(88)) | 恒等式 < 1e-12、非零性・符号の全域確認 |
| C4 | App K 実働バーン: 放射質量比 ≈ 51% を再現 | 論文表示桁で一致 |
| C5 | 運動学的天井: λ < ½(1−x) (v2: Eq.30) | 閉形式一致 < 1e-12 |
| C6 | 制御則の角度分布: 4πn² = −ṁ − 3ma·cosθ の monopole/dipole モーメントが −ṁ / ma を返す | 求積で < 1e-10 |

許容誤差の原則: 閉形式は相対 1e-10 以下、論文の数値値は表示桁一致。

## 5. AUTHORITY LEVELS — G2: 全数値に三色ラベル

Le の論文自身が権威水準を区別している。成果物はこれを**相続**する。
航路表・図・レポートのすべての数値に以下のラベルを付ける:

- **[R]** 厳密閉形式(例: 天井 ½(1−x)、Tsiolkovsky、制御則)
- **[N]** 論文の数値的下界に依拠(例: フロンティア g̲(x))。
  **サンプル範囲外への外挿は禁止**
- **[H]** 発見法・経験則(例: 包絡線 ½(24/25−x))。
  Le 自身が「heuristic であり厳密な不等式ではない」と明記している

拘束の採用方針(Phase L ゲートで確定、L7 準拠・三層フロンティア):
主拘束は [R] の天井 λ < ½(1−x)(Λ>0 では ½(1−x−y))と
厚壁窓 x_eff = x+2λ < 4/5。感度帯は [N] の g̲(x)(x ∈ [0.1, 0.7]、
外挿は実装で禁止)。厳密安全床は [R] の c(x)(v3 (48)/(73)、~10⁻⁴ 台)—
航路表の各点に併記するが運用拘束にはしない。λ_DEC 系は診断・認証専用で
拘束体系に入れない(L8 参照)。時間最適プロファイルは
[R] 床 / [N] 実効 / [R] 天井の三層区間で提示する。
非線形接合ソルバーの再実装はしない(明示的な却下済み分岐である。
再検討したければ人間に提案せよ)。

## 6. INDEPENDENT PATHS — G3: 主要量は二経路で計算

- Bondi 予算: 閉形式積分 vs `warpax.bondi` 数値
- 測地線距離: arccosh(−v₀·v₁) vs ODE 積分の弧長
- 光度曲線: 解析形 vs 数値微分
- 遅延時間写像(観測者系): 独立実装 2 系統

二経路の不一致は **STOP 事由**。片方を黙って捨てるな。

## 7. FORBIDDEN BEHAVIORS

- 排気の微視的物理・光子スペクトルを主張すること。ボロメトリック
  +パラメータ化シナリオに限る(Le 論文と同じ自制)
- 摂動的結果(小加速度の Theorem 2 等)を有効域外へ延長すること
- 認証テストの不一致を定数調整で「直す」こと。不一致は結果であり、
  STOP して報告する。論文側の誤りの可能性も含めて報告してよいが、
  **論文が誤っていると結論する権限は人間側にある**
- 検出可能性を特定観測装置の性能予測として断定すること
  (スケーリング則と桁の議論に留める)
- Füzfa が既に計算したもの(点ロケットの軌道・エネルギーコスト・
  アベレーション・ドップラー)を新規結果として提示すること。
  引用して使い、差分(乗客殻の許容性制約)を明示する
- AI 自身の記憶に基づいて新規性・式形を判定すること。必ず
  ローカル PDF と文献検索による
- 結果が地味であることを理由に権威ラベルを格上げすること

## 8. STOP & REPORT — 即停止して人間に報告する条件

1. 認証テスト不合格が **2 回の修正試行**で解消しない
2. 二経路計算の不一致(G3)
3. **論文の記述間の矛盾を発見した**(これは事故ではなく発見候補。
   本プロジェクトは Le の定理群の世界初の独立精読でもある)
4. ASSUMPTION なしで進めない分岐に到達した
5. 規約台帳の変更が必要になった
6. 新規性を脅かす先行研究を発見した

報告フォーマット: 何を・どこで・再現手順・影響範囲・提案する選択肢。

## 9. FREE TO DECIDE — AI の裁量

数値積分器の選択 / コード設計・モジュール分割 / テスト設計 /
最適化・求根の手法 / 可視化手法 / 機動カタログの内部構成 /
計算の並列化(ただし本プロジェクトに長時間ループは想定されない。
1 プロファイル数秒の世界である。数十分を超える計算を設計した時点で
何かが間違っている——STOP して見直せ)

## 10. PHASES と人間ゲート

| Phase | 内容 | ゲート成果物 |
|---|---|---|
| L | 文献の完結(v3 精読・式抽出・被引用スキャン) | 差分声明+式台帳 |
| 0 | 検証ハーネス(認証セット全緑) | 認証レポート |
| 1 | 航路カタログ(ODE 積分・拘束付き機動) | 航路表+図 |
| 2 | 観測署名(光度曲線・角度・検出スケーリング) | 署名カタログ |
| 3 | 論文化+Three.js 航路シミュレータ | 原稿+アプリ |

各フェーズの完了時に人間が承認するまで次フェーズに着手しない。

## 11. DELIVERABLES 配置

```
astrogation/
  CLAUDE.md            ← 本ファイル(不変)
  conventions.md       ← 規約台帳(Phase 0)
  ASSUMPTIONS.md       ← 仮定台帳(随時)
  docs/phases/         ← フェーズ指示書
  docs/reports/        ← ゲート成果物・STOP報告
  papers/              ← オラクル PDF
  src/astrogation/     ← ライブラリ本体
  tests/               ← 認証テスト(pytest)
  results/             ← 航路表・図・カタログ
```

## 12. 引用義務

```
Le, An T. "Steering a warp drive without exotic matter."
arXiv:2606.22531 (2026).

Le, An T. "On the boundary cost of source-consistent warp shells."
arXiv:2605.25417 (2026).

Le, An T. "Observer-robust energy condition verification for warp
drive spacetimes." arXiv:2602.18023 (2026).

Füzfa, A. "Interstellar travels aboard radiation-powered rockets."
Phys. Rev. D 99, 104081 (2019).

Damour, T. "Photon rockets and gravitational radiation."
Class. Quantum Grav. 12, 725–738 (1995). arXiv:gr-qc/9412063.
```

warpax は MIT ライセンス。使用時は引用。

---

## 合言葉

> 値段は決まった。次は料金表と時刻表を作る。
> 論文がオラクル。認証なき式は存在しない。
> 権威水準は相続する。[R] を [N] で汚さず、[H] を [R] と偽らない。
> 不一致は事故ではなく、最初の発見候補である。

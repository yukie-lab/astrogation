# P1 監査メモ — G3 初回超過 14 行と 4 修正の生成経路影響(ゲート追加作業 2)

作成日: 2026-08-08。対象: 初回 G3 ゲート実行(14 行超過、図なしで停止)→
最終実行(306 行全合格)の間に入れた 4 修正の分類と、カタログ値への影響。

## 1. 4 修正の分類(生成経路に触れたか)

| # | 修正 | 経路 | カタログ値への影響 |
|---|---|---|---|
| (i) | 表A の質量検証 ODE のステップ数を η に比例化 | **検証のみ**(G3 経路 B) | ゼロ(表A 値は閉形式 (15)。G3 誤差列のみ 1.6e-8→7.5e-13 に改善) |
| (ii) | [N] フォールバックのイベント着地を枝凍結 RK4 化 | **検証のみ**(`ride_time_ode` は G3 経路 B 専用) | ゼロ(T_over_R は求積 = 経路 A から出力) |
| (iii) | 求積の区間分割に Γ の max 枝を追加+長スパン区間の分解能比例化 | **生成に接触**(`ride_time_quadrature` は T_over_R の生成経路) | **有**: 該当行の T_over_R が最大 ~1.6e-8 相対でシフト(表示 4 桁では不可視)。↓§2 |
| (iv) | 極小 x の (s−1) 桁落ち除去(σ₀/M₀ を x/(1+s) 形に) | **生成に接触**(c 鎖 → 床 tier の T_over_R) | **初回ゲート合格版には混入せず**: 本修正は初回の完全実行(14 行版)より**前**に適用済み。↓§3 |

## 2. 修正 (iii) で値が動いた行(初回 14 行版 → 最終版)

生成値(T_over_R)が動いたのは求積が枝キンク/長スパンを跨いでいた行のみ。
シフト量は当該行の初回 G3 誤差と同程度(求積側が律速だったため):

| 行 | シフト(相対) |
|---|---|
| (0.5, 0.24, floor) | ~1.4e-8 |
| (0.7, 1.0, floor), (0.7, 2.0, floor), (0.5, 1.0, floor) | ~1.0〜1.8e-8 |
| (0.7, 12.0, ceiling) | ~1.6e-8 |
| 上記以外の 76 行 | < 1e-8(表示桁に影響なし) |

初回版と最終版の `timeopt_bracket.md`(x₀=0.3、4 有効数字)は**全行一致**を
目視照合済み。effective 行の初回超過(~1e-6)は修正 (ii) = 検証側のみで
解消しており、生成値は不変。

## 3. 修正 (iv) の履歴上の注意(完全性のための記録)

(iv) は「初回の完全 G3 ゲート実行」よりも前(TierDomain クラッシュ後の
デバッグ段階)に適用された。適用前の床 tier 大 Δη の求積値は tail ノイズで
最大 ~16% ずれ得た(例: Δη=12, x₀=0.3 の床 T: 5.13e18 → 5.98e18)が、
**この誤値はどの G3 合格カタログにも掲載されていない**(クラッシュした
実行は出力未完、初回 14 行版の時点で修正済み)。

## 4. C1–C8 の無変更確認

- 認証テストのコード(`test_certification.py`)は Phase 1 を通じて**無変更**
- 修正 (iv) は shell.py の実装形を変えたが、厳密な代数的恒等変形
  ((s−1)(s+1) = −x)であり、C2 の三者一致テスト(直接形 vs 因数分解 vs M₀、
  < 1e-12)が同一性を担保
- 各修正段階でスイート全体を再実行し、**C1–C8 は全段階で全緑**
  (最終確認: 40 passed + 考古学 6 skip + xfail 1、52 s)

## 5. 結論

初回 14 行の超過はすべて **G3 検証側または求積の数値精度**の問題であり、
物理恒等式の不一致は一件もなかった。生成値の変動は最大 1.6e-8 相対
(表示桁不可視)。修正はいずれも定数調整ではなく、収束次数の回復
(分割点・分解能・桁落ち・枝凍結)である。

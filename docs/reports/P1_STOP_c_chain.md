# STOP 報告 — c(x) 定数鎖の (73) 表示桁再現不能(CLAUDE.md §8-1)

日付: 2026-08-08(Phase 1、タスク 1-1)
報告形式: 何を / どこで / 再現手順 / 影響範囲 / 提案選択肢(CLAUDE.md §8)

## 何を

PHASE_1.md 1-1 の要求「c(x) は v3 (48) 一式(…5 点で回帰テスト)」に対し、
v3 App C の定数鎖を実装したが、**(73) の 5 点値
{8.5, 14, 17, 14, 2.0}×10⁻⁵ の表示桁再現が 2 回の修正試行で達成できなかった**。

## どこで(原因の所在)

鎖の構成要素のうち、**印字された閉形式はすべて実装・検証済み**
((48)、Γ (68)、δ_Q (71)、A/C_shape (72)、S_λ、κ、ϱ_Q、r_⋆ 方程式 —
r_⋆ 方程式は polydisc 保持条件から厳密に再導出できることを確認、台帳 L41)。
再現を阻むのは **NL_A・NL_B・B⋆_m の 3 量**で、v3 は手続き的記述のみで
閉形式を掲載していない(p.36: "the suprema on the polydisc of the two rows
carrying nonlinearity"、p.37: "the supremum of the margin on that polydisc")。

## 試行の記録(再現手順つき)

再現: `warpax` 環境で `python -c "import sys; sys.path.insert(0,'src');
from astrogation.appc_floor import chain_report, c_floor_conservative; ..."`
または pytest の xfail テスト `test_regression_c73_display_digits`。

| x | (73) 論文値 | 試行1(主読み・grid上限) | 比 | 試行2(保守化・解析上限) | 比 |
|---|---|---|---|---|---|
| 0.1 | 8.5e-5 | 4.12e-4 | 4.85 | 6.22e-5 | 0.73 |
| 0.2 | 1.4e-4 | 7.31e-4 | 5.22 | 9.35e-5 | 0.67 |
| 0.3 | 1.7e-4 | 7.34e-4 | 4.32 | 1.01e-4 | 0.60 |
| 0.5 | 1.4e-4 | 2.47e-4 | 1.77 | 6.75e-5 | 0.48 |
| 0.7 | 2.0e-5 | 8.12e-6 | 0.41 | 4.30e-6 | 0.21 |

- 試行 1(主読み、ASSUMPTIONS A3): distinguished boundary 上の grid 最大。
  桁と山型の形状は一致するが、束縛枝が ½δ_Q(論文は「x ≲ 0.7 で 2 次根」)
- 試行 2(保守化): 上限を三角不等式の**解析的過大評価**に置換。
  **全 5 点で論文値より下**(下界としての整合)、**束縛枝も論文記述と一致**
  (x ≲ 0.7 で 2 次根、0.7 で δ_Q 系)。ただし表示桁は 0.2〜0.73 倍

## 切り分け表

| 仮説 | 判定 | 根拠 |
|---|---|---|
| 転記ミス(台帳/実装) | **可能性低** | 印字閉形式は全て再検証済み(r_⋆ は独立再導出、(81)(88) 等は Fraction 恒等で認証済み)。保守版の枝構造・桁・形状・「全点下側」は整合的 |
| 実装解釈の相違(A3) | **可能性高** | NL_A/NL_B/B⋆_m の関数形自体が非掲載。過大評価の度合いで 0.2〜0.7 倍は自然に説明可能 |
| 論文側の記述不足 | **発見候補** | (73) は「論文本文だけからは再現不能」— 著者コード(Data availability: github.com/anindex/world_tube)にのみ具体形がある可能性。これは v3 の再現性に関する正当な指摘(CLAUDE.md §8-3 類縁) |
| 論文値の誤り | 判定不能 | 権限外(CLAUDE.md §7)。上の 3 行の解消後にのみ検討可能 |

## 影響範囲

**床 tier のみ**。表A・表B・実効 [N]・天井 [R]・時間最適の実効/天井層・
ミッションプロファイル・SI レイヤーは非依存(L8 依存グラフのとおり)。

## 暫定措置(裁定までの運用)

床 tier は**試行 2 の保守化鎖 c_cons(x)** を採用し、全成果物で
**[R(A3)/STOP-pending]** ラベルを明示。根拠: (i) A3 解釈の下で全ステップが
印字閉形式または証明可能な過大評価 → 真の下界、(ii) 論文自身の 5 点より
全点で下 = 論文が正しくても安全側、(iii) 連続関数なので騎乗プロファイルが
構成可能(論文 5 点の補間では区間内の保証がない)。

## 提案選択肢(人間の裁定を求める)

1. **c_cons を [R(A3)] 床として採択**(暫定措置の追認。推奨 — 保守側で安全)
2. 論文 5 点の [N] 補間に置換(認識論的には g̲ と同格になる。憲法 §5 の
   「[R] の c(x)」の文言と不整合が生じる)
3. **著者コード world_tube を取得して NL_A/NL_B/B⋆_m の具体形を照合**
   (再現に最短。照合結果次第で (73) の完全再現が可能になる見込み)
4. 床 tier を保留し当面二層運用([N] 実効 + [R] 天井)

選択肢 3 は同時に「(73) は本文のみから再現不能」という**発見候補の検証**を兼ねる。

---

## 追記(2026-08-08 ゲート裁定 1: 選択肢 3 採択 → 実施済み)

人間裁定により選択肢 3(著者コード world_tube 照合)を実施した。
結果: **(68)-(73) の鎖は公開著者コードにも存在せず、条件 (c) が発動 —
STOP 継続、発見候補は「本文+公開コードから再現不能」に昇格**。
詳細・相互検証 6 件(D(x) 撤回の著者明文確認を含む)・次の一手の選択肢は
`P1_worldtube_reconciliation.md`。床 tier は条件 (a) により
c_cons [R(A3)/STOP-pending] を継続。

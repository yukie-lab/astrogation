# CONVENTIONS — 規約台帳

**状態: 提出(2026-08-08、人間承認待ち)。変更は人間の承認必須(CLAUDE.md §3)。**
出典の原則: 本台帳の物理規約はすべて v3(arXiv:2606.22531v3)・warpax と同一。

## 1. 幾何・単位

- **G = c = 1**。計量符号 **(−,+,+,+)**。**κ = 8π**(Le・warpax と同一)
- 幾何単位系の基底は**長さ(メートル)**。質量 m_geo = G m_SI/c²、時間 t_geo = c t_SI
- 単位換算は `src/astrogation/units.py` に**一元集約**。他モジュールでの定数直書き禁止
- 換算定数(定義値・公認値):
  - c = 299 792 458 m/s(SI 定義、厳密)
  - G = 6.674 30×10⁻¹¹ m³ kg⁻¹ s⁻²(CODATA 2018)
  - GM☉ = 1.327 124 400 18×10²⁰ m³ s⁻²(IAU 2015 nominal)→ 太陽質量の幾何長 GM☉/c² ≈ 1476.6250 m
  - 1 Julian yr = 31 557 600 s(厳密)、1 ly = c·yr = 9 460 730 472 580 800 m(厳密)

## 2. 角度・時間・座標

- **ϑ は各遅延カットの瞬間静止系で、固有加速軸から測る**(v3 (13) の規約)。
  前方極 ϑ = 0(n² 最小。飽和時に厳密ゼロ)、**後方極 ϑ = π が排気**(n² 最大)。
  固定系の角度パターンはこの静止系双極子のアベレーション(v3 App A)
- **u は遅延時間**(Kinnersley/RT–dS の場合、Γ 上の固有時間と一致、v3 Lemma 2 証明)
- KRT 座標 (u, r, ζ, ζ̄)、P 規格化は K = 1(AC − BB̄ = 1/2、v3 (6))

## 3. 記号台帳

| 記号 | 定義 | 出典 |
|---|---|---|
| m(u) | Bondi 質量 | v3 (4)-(5) |
| x | 2m/R | v3 (25) ほか |
| s | √(1−x) | v3 (24) |
| λ | \|a\|R | v3 (30) |
| μ | \|ṁ\|R/m | v3 (30) |
| x_eff | x + 2λ(後方極実効コンパクト度) | v3 (40),(60) |
| y | H²R² = (Λ/3)R² | v3 (20) |
| Δη | ラピディティ利得 = ∫\|a\|du(直線) | v3 (15) 直下 |
| n² | 排気ヌルダスト振幅(4πn² = −ṁ − 3ma cosϑ) | v3 (7),(13) |
| Φ(λ,μ) | worst-observer Type-I 表面 DEC マージン | v3 (35) |
| δ₀, M₀ | アンカーマージン、8πRδ₀ = M₀(x) | v3 (61) |

## 4. 式引用規則

- 実装する式には **v3 式番号をコード内 docstring/コメントで必須併記**
- v2 への言及は歴史的文脈のみ、`[v2:]` 前置で明示(実装は不可)
- 式の唯一の供給源は式台帳 `docs/reports/L2_formula_ledger.md`(Phase L ゲート成果物)。
  台帳にない式を記憶から補完しない

## 5. 権威ラベル(CLAUDE.md §5 の運用形)

- **[R]** 厳密閉形式。認証テスト合格が使用条件
- **[N]** 論文の数値に依拠。**サンプル範囲外への外挿は実装レベルで禁止**
  (`frontier.g_lower` は範囲外で例外送出)。g̲(x): x ∈ [0.1, 0.7]
- **[H]** 発見法。**拘束・判定への使用禁止**(表示・比較のみ)。包絡線 (39)
- **[v2-retracted]** v3 で構造ごと撤回された v2 固有量(現登録: D(x) 一式 v2 (55)-(61))。
  `src/` での使用禁止(`tests/test_v2_retracted_guard.py` が強制)。
  `tests/archaeology/` のみ(2026-08-08 人間承認、L6)
- 診断量(λ_DEC 系、v3 Lemma 5)は [R] だが**拘束としての使用禁止**(L7 §B)

## 6. 許容誤差(CLAUDE.md §4 の実装形)

| 対象 | 基準 |
|---|---|
| 閉形式恒等式(C3 系含む) | 相対 < 1e-12 |
| 閉形式一般 | 相対 ≤ 1e-10 |
| 求積(C6) | < 1e-10 |
| 二経路一致(C8 以降、G3) | 相対 < 1e-8 |
| 論文数値 | 表示桁一致(丸めて一致) |
| 「厳密ゼロ」を主張する認証 | `fractions.Fraction` による有理厳密評価 |

## 7a. 行き先カタログ(Phase 1 追記、PHASE_1.md 1-3 の指示で固定)

| 行き先 | 距離(カタログ固定値) | 典拠(丸め前) |
|---|---|---|
| プロキシマ・ケンタウリ | **4.25 ly** | 視差 768.5 mas(Gaia 系)→ 4.2465 ly |
| TRAPPIST-1 | **40.5 ly** | 視差 80.45 mas → 12.43 pc ≈ 40.54 ly |
| いて座 A*(Sgr A*) | **2.6×10⁴ ly** | R₀ = 8178 pc(GRAVITY 2019)≈ 2.67×10⁴ ly |
| アンドロメダ銀河(M31) | **2.54×10⁶ ly** | 765 kpc(Cepheid 系)≈ 2.50×10⁶ ly |

固定値は PHASE_1.md の指示値。距離の不確かさ(数 %)はカタログの目的
(桁とスケーリング)に対して無視できる。変更は人間承認必須。

## 7. 実行環境(Phase 0 確定)

- conda 環境 **`warpax`**(`/Users/yukie/miniforge3/envs/warpax/bin/python`):
  Python 3.12.13 / numpy 2.4.6 / scipy 1.18.0 / pytest 9.1.1 / jax 0.10.2 / **warpax 1.3.0**
- 本体ライブラリは numpy 以外に依存しない(jax 不使用)。warpax はクロスチェック専用
- テストスイート全体 < 1 分(超過は STOP、CLAUDE.md §9)

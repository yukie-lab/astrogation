# ASSUMPTIONS — 仮定台帳

> CLAUDE.md §2: 論文にない主張・補間・近似をコードに入れる場合は、
> 必ずここに番号付きで記録し、根拠と影響範囲を書く。

---

## A1. 検証範囲声明(Verification Scope Statement)

**登録日/承認**: 2026-08-08、Phase L ゲートにて人間承認。

**声明**:

1. **数値認証の対象**は、L8 依存グラフの【W】閉形式証人と【E】厳密恒等式に
   限る。許容誤差は CLAUDE.md §4 の原則(閉形式は相対 1e-10 以下、論文数値は
   表示桁一致。C3 系の恒等式は < 1e-12)。
2. **【S】構造定理は数値認証しない**。対象: Prop. 8 の陰関数定理(Lyapunov–
   Schmidt)、Cohn–Vossen/Weyl 剛性、Schauder 評価 (75)-(76)、Lemma 7 の楕円
   角シンボル論、Picard–Lindelöf/Grönwall (90)、Ovsyannikov 解析 majorant。
   これらは v3 の証明を証明レベルで受容する。その**定量的な影**(δ_Q (71)、
   c(x) (73) の 5 点、Kantorovich h)は補助認証で再現し、間接的に押さえる。
3. **[v2-retracted]** に登録された対象(現在: v2 (55)-(61) の分岐スカラー
   D(x) 一式)は、`tests/archaeology/` の隔離テストでのみ扱い、`src/` での
   使用は `tests/test_v2_retracted_guard.py` により機械的に禁止する。
4. **サンプル範囲の遵守**: g̲(x) は x ∈ [0.1, 0.7] の外で使用しない([N]、
   外挿禁止)。摂動的結果は各々の有効域(Thm 2: 小振幅 / Thm 4: 遅い固定軸
   sub-horizon バーン / App F: 断熱 O(μ)、実働バーンは ωR ~ 0.25 で縁)の
   外へ延長しない。λ_DEC 系は診断であり拘束に使わない。
5. **二経路検証(G3)の対象**: Bondi 予算(閉形式 vs `warpax.bondi`)、
   H³ 測地線距離(arccosh 閉形式 vs ODE 弧長)、光度曲線(解析 vs 数値微分)、
   遅延時間写像(独立 2 実装)、および (35) worst-observer マージン
   (自前実装 vs warpax の観測者ロバスト EC 枠組み)。不一致は STOP。
6. 本プロジェクトは v3 の定理群の**証明の独立再導出を行わない**。行うのは
   (i) 閉形式の認証、(ii) 論文数値の再現、(iii) 二経路整合、(iv) 依存グラフ
   (L8)に基づく荷重ノードのみの使用、である。

**根拠**: L6(C3 再定義)、L7(c(x)/λ_DEC の地位)、L8(依存グラフと
カットセット分析)。いずれも 2026-08-08 人間承認。

**影響範囲**: Phase 0 の pytest 構成全体(認証セット C1–C6 の実装形)、
Phase 1–2 の全成果物の権威ラベル([R]/[N]/[H]/[v2-retracted])の付与規則。

---

## A2. 実働バーン(C4)の無次元閉包

**登録日**: 2026-08-08(Phase 0)。

**仮定**: v3 Supplement の実働バーンは (x⋆, λ_max, Δη) = (0.3, 0.12, 0.24) の
3 パラメータで無次元的に閉じるとして再現する。すなわち τ := (u−u₀)/(u_f−u₀) で
  ∫₀^u a du′ = Δη·(τ − sin(2πτ)/(2π)),  λ(τ) = λ_max sin²(πτ),
  x(τ) = x⋆·exp(−3Δη·(τ − sin(2πτ)/(2π)))
とし、a_max と burn 時間 T の個別値(論文 Fig. S3 の軸スケール)には依存しない。

**根拠**: a(u) = a_max sin²(πτ) と飽和予算 m = m₀e^{−3∫|a|} の下で、放射質量比・
x_eff(τ)・λ(τ) は厳密に (x⋆, λ_max, Δη) のみの関数(スケール不変性、初等計算)。
論文明記値 Δη = 0.24 が a_max·T = 2Δη を固定する。

**影響範囲**: 認証 C4 のみ。再現結果: 放射 51.32% → 表示桁 51% 一致、
x_eff^peak = 0.4597 → 表示桁 0.46 一致(P0_certification.md §1)。

---

**Phase 0 で置いた仮定: A2 のみ**(A1 は Phase L ゲートの検証範囲声明)。

(以降、コードに仮定を導入するたびに A3, A4, … として追記する)
